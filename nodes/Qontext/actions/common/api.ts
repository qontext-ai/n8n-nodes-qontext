// Base paths for the Qontext API, kept in one place so a version bump touches a single file.
// The workspace is implicit in the API key and never appears in a URL.

export const FILES_URL = '/v1/files';
export const FOLDERS_URL = '/v1/folders';
export const SEARCH_URL = '/v1/search';
